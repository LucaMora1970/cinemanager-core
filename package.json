{
  "name": "cinemanager-core",
  "private": true,
  "version": "1.0.0",
  "description": "Netlify functions per cinemanager-core",
  "scripts": {
    "deploy:rules": "firebase deploy --only firestore:rules"
  },
  "dependencies": {
    "nodemailer": "^6.10.1"
  }
}
